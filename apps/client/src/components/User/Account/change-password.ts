/** Texts of the change-password dialog (#88), shared with its specs. */

/** Shown on the login page once the change has gone through. */
export const PASSWORD_CHANGED =
    'Password changed. Please log in with your new password.';

/** Shown when a draft page's "Stay" kept the user logged in. */
export const PASSWORD_CHANGED_STAYED =
    'Password changed. Log out and log in with your new password when you are done here.';

/** On the current-password field when the API answers USER_WRONG_PASSWORD. */
export const WRONG_CURRENT_PASSWORD = 'The current password is incorrect';
